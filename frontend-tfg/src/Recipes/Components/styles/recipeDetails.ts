import {CSSProperties} from "react";

export const container : CSSProperties = {
    textAlign: "center",
}

export const row: CSSProperties = {
    marginTop: "10px",
    marginBottom: "10px",
}

export const recipeData: CSSProperties = {
    marginLeft: "10px",
    marginRight: "10px",
    textAlign: "left",
}


export const recipeButtons: CSSProperties = {
    justifyContent: "center",
    display: "flex",
    flexDirection: "column",
    gap: "20px"
}

export const commentsForm: CSSProperties = {
    textAlign: "justify",
    display: "flex",
    flexDirection: "column",
    verticalAlign: "middle",
}
