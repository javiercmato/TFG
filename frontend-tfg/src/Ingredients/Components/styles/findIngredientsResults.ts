import {CSSProperties} from "react";

export const titleRow: CSSProperties = {
    textAlign: "center",
}

export const resultsRow: CSSProperties = {
    justifyContent: "stretch"
}

export const resultItem: CSSProperties = {
    textAlign: "center",
}

export const pagerRow: CSSProperties = {
    justifyContent: "center"
}
