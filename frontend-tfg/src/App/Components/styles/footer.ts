import {CSSProperties} from "react";

export const footer: CSSProperties = {
    width: "100%",
    position: "fixed",
    bottom: 0,
    padding: "5px",
    background: "#D7415D",
    textAlign: "center",
    height: "auto",
}

export const footerColStyle: CSSProperties = {
    display: "flex",
    justifyContent: "center"
}
