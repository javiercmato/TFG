/** Posibles criterios para realizar una búsqueda con parámetros */
type SearchCriteria = {
    name?: string,
    type?: string,
    page?: number,
    pageSize?: number
}

export type {SearchCriteria};
