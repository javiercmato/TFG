import {CSSProperties} from "react";

export const cardHeader: CSSProperties = {
    textAlign: "center",
}

export const avatarPreview: CSSProperties = {
    maxWidth: "40%",
}

export const row : CSSProperties = {
    padding: "1%",
}

export const formGroup: CSSProperties = {
    display: "flex",
    justifyContent: "center",
}
